{
    "tasks": [
        {
            "type": "shell",
            "label": "make_solver",
            "command": "${workspaceFolder}/.vscode/make_solver.sh",
            "args": [],
            "options": {
                "cwd": "${workspaceFolder}/build"
            },
            "problemMatcher": [
                "$gcc"
            ],
            "group": {
                "kind": "build",
                "isDefault": true
            }
        }
    ],
    "version": "2.0.0"
}